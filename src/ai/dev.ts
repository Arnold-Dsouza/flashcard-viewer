// Local AI mocks for flashcard generation
// This file loads the mock AI service for flashcard generation

import { ai } from './mock-ai-service';

// This line is just to ensure the AI service is loaded properly
console.log('AI mock service initialized for flashcard generation');

// Export for direct use in other files
export { ai };
